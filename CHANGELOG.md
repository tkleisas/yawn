# Changelog

*Each release was implemented by saying "do this" and then saying "no, not like that" between 2 and 47 times. Versions are set automatically by the git tag.*

## Recent releases

| Version | Summary |
|---|---|
| **v0.87.4** | **Race-free structural scene edits: the UI↔audio quiesce handshake.** `App::stopAllClipsForSceneEdit()` — which runs before scene insert/delete/duplicate, all of which reallocate the per-track clip-slot vectors — used to fire unquantized stops and mutate immediately: the engine processed the stops asynchronously, leaving a ~1-callback window where a callback already past `processCommands` could still dereference the old slots' cached `clipAutomation` lanes, with the 5-second clip graveyard as a hope, not a guarantee. Now: `sendCommand()` increments an atomic produced-counter (release) after each successful push, and `processCommands()` snapshots that counter at drain *start* and publishes it as consumed (release) at drain end — the new `AudioEngine::quiesceCommands()` then spins on the UI thread (250 ms timeout + warning; graveyard stays as memory-safety backstop) until consumed ≥ the post-send produced count, which by release-sequence ordering proves the stops were actually popped. Three engine states handled: stream stopped (no audio thread → return), suspended (the drain can't run → wait out one heartbeat; the queued stops are consumed before rendering resumes), running (~1 callback period). Same pass closed two real holes in `MidiClipEngine`: `scheduleStop(QuantizeMode::None)` never dropped `m_pending` — a quantized launch queued before an immediate stop fired at the next bar and re-pointed the track at a since-graveyarded clip (`ClipEngine` already cleared its pending slot); and `stopNow()` kept the stale `clipAutomation` pointer cached — now nulled, matching `ClipEngine`. New tests: `AudioEngineQuiesce.*` (the counter plumbing, driven headlessly via `pumpInputForTest`), `MidiClipEngineTest.ImmediateStopDropsQueuedPendingLaunch`, `...ImmediateStopClearsCachedAutomationPointer`. Also new: `docs/ai-sound-design.md` surveys AI/ML integration (CLAP library tagging, audio→patch sound matching, text→patch LLM loop) ranked by reachability, with a prototype plan. All 1465 tests pass (1 skipped placeholder) |
| **v0.87.3** | **Integration testing: the UI command channel learned to answer questions, and CI now drives the real app.** (1) **State queries** — new `get` family on the `YAWN_CMD` TCP channel: `tracks`, `scenes`, `tracktype`, `instrument`, `param` (float readback), `playing`, `bpm`, `view`, `modal`, `undo`/`redo` (stack state + description) — test scripts can finally *assert*, not just poke. (2) **New verbs** — `undo`/`redo` through the real app path (verified live: add track → 6 tracks → undo → 5 → redo → 6), `new` (fresh project without the dirty-check modal headless scripts can't answer), `wait <frames>` (deferred ack = deterministic frame sync, no blind sleeps), `quit <code>` (the process exit code carries pass/fail). (3) **Robustness** — `shot` creates missing parent directories (a stale `/tmp` broke it mid-session), the startup bind retries for a few seconds so back-to-back restarts don't lose the port to a dying instance, and `shot`/`wait` reject a second deferred request with `ERR busy` instead of silently stealing the ack. (4) **End-to-end harness** — `scripts/ui_smoke.py` launches the real app and runs 30 assertions through the channel (queries, undo/redo, screenshot PNG validation, modal open/close, clean exit); wired into CI as a "UI smoke (Xvfb)" step on the Linux job, so the real UI now runs in every build. All 1464 tests pass (ASan suite green) |
| **v0.87.2** | **UI polish, batch 3: affordances — things finally look like what they do.** (1) **Cryptic buttons**: the session corner's bare red square (stop-all-clips) now reads "Stop" next to the square; the master strip's overlaid square icon — indistinguishable from a broken checkbox — is a labeled "Stop All" button; the transport's bare "LINK" text (status? button?) now carries its state ("LINK off" / "LINK on" / peer count) in a wider pill; the piano-roll clip-ops column widened and "Clr" is spelled "Clear". (2) **Mixer**: the left-column show-toggles "S"/"R" now read "Snd"/"Rtn" (per-strip M/S/R stays — that's standard mixer vocabulary), and the return/master fader+meter groups are centered in their columns instead of hugging the left edge with a dead right half. (3) **Preferences**: the 2-control Defaults tab and 1-control Theme tab — each ~90% empty — are merged into one Defaults tab with help text, one tab fewer in the strip. (4) **Visual knobs A–H**: knobs the current shader doesn't actually read now render dimmed (the engine reports live uniform locations via new `VisualEngine::isLayerKnobUsedByShader`), so the letters that do something stand out; "+ Add Pass" pill is sized to its label instead of the text touching the rounded ends. All 1464 tests pass (ASan suite green) |
| **v0.87.1** | **UI polish, batch 2: copy fixes and small-panel clarity.** (1) **About dialog** reads "Yet another Audio Workstation New" — the missing spaces finally got their spaces. (2) **Browser Files**: the add-library button was a bare "+" while the empty-list hint told you to "Click '+ Folder'" — the button now reads "+ Folder", sized to its label. (3) **Browser Files/Presets/Loops lists** no longer clip the last row's text mid-glyph at the panel bottom; only fully-visible rows are drawn. (4) **Arrangement track headers**: the cryptic 36 px "Ses"/"Arr" state label is now a 64 px [Ses\|Arr] segmented control showing both modes with the active one lit — clicking a segment selects that mode directly (undo-wrapped, no-op on the active segment). (5) **Preferences → MIDI** no longer lists our own RtMidi ports: every input we open registers an ALSA client that then showed up in the *Outputs* list ("RtMidi Input Client", enabled by default — a built-in feedback loop). Our client is now named "YAWN" and filtered out of both port enumerations, and the port-open path maps the UI's filtered index back to the raw RtMidi index so the right port still opens. Also fixed the v0.87.0 CI break: hash-pinned FetchContent deps (SDL3, stb, minimp3, eigen) can't shallow-clone (`fatal: unable to read tree` on fresh runners) — they now clone fully. All 1464 tests pass (ASan suite green) |
| **v0.87.0** | **UI polish: a scriptable TCP UI command channel, an FM Synth algorithm display you can actually read, and a menu-bar bug that ate your clicks.** (1) **UI command channel** — set `YAWN_CMD=<port>` and the app listens on 127.0.0.1 for line-based verbs (`ping`, `shot`, `key`, `click`/`rclick`/`dclick`/`mousemove`, `view`, `menu`, `menuitem`, `addtrack`, `addinstrument`, `addeffect`, `setparam`, `dialog`, `dbg`, `quit`) so scripts drive the UI semantically instead of synthesizing X11 input; `shot` reads the GL back buffer, so captures work under Wayland too. New `util::TcpLineServer` lives in yawn_core with socket-level unit tests; `scripts/ui_probe.py` is the reference client. Menu actions (add track, add instrument/effect by name, dialog openers) were factored into shared App methods so menu items and the channel run one implementation with one undo shape. (2) **Swallowed-click fix** — the first click on a menu title after a menu or dialog closed did nothing: `FwMenuBar` kept a stale open-index when its menu closed externally (Escape, item activation, outside click, right-click menu replacement), so the next hover phantom-opened a menu and the click "re-clicked" it shut in the same millisecond. `ContextMenuManager` now exposes a generation counter bumped on every chain show/teardown and the bar only trusts its state while the open chain is the one it pushed; `LayerStack` dispatch also copies handlers before invoking since dialogs/menus remove their own entry mid-call. (3) **Menus read properly** — shortcut column gap 8→16 px plus a wider chevron gutter ("Insert SceneIns", "Record Length: Unlimited▸" were glued; the Scene item had also embedded a literal tab in its label), and separators now paint in the visible `border` color (they were 3 RGB units off the popup background). (4) **Dialogs** — the message scrollbar was effectively invisible (3 px at alpha 30); now wider and brighter with ▲/▼ scroll affordances, so the Keyboard Shortcuts sheet is discoverably scrollable at small window heights. (5) **FM Synth algorithm display** — taller and vertically aligned with the knob grid (new `alignsToKnobGrid` widget hook), with a reworked operator-graph layout (carriers under modulators, output labels, feedback arrows) covered by layout unit tests; all 8 algorithms verified at width. All 1461 tests pass (ASan suite green) |
| **v0.86.14** | **Perf: the last locks and heap allocations are off the real-time audio path.** Six audit items closed. (1) **Ableton Link** captured and committed *app* session state on the audio callback — those calls take internal locks; the commit now uses Link's realtime-safe `commitAudioSessionState()` on the already-captured audio state (`numPeers`/`clock` were RT-safe all along, per the headers). (2) **VST3 parameter handoff** locked a mutex and heap-allocated on both sides of every block — now an SPSC ring (`util::RingBuffer`, 4096 slots) for UI→audio, plus a plain list for calls already on the audio thread (automation playback, MIDI-learn), selected via a new `rt::onAudioThread` marker set by `processAudio`; the `ParameterChanges` handed to the plugin are persistent per-device objects whose queues are rebuilt in place instead of constructed per block. (3) **SplineEQ** allocated 4 FFT scratch vectors every 1024 samples — now members sized in `init()`. (4) **Automation recording** allocated per breakpoint — arming Touch/Latch reserves lane + envelope storage up front (one-off per arm gesture), and newly created lanes start with 4096 reserved points, so mid-take `addPoint` never grows. (5) **MIDI held-note tracking** churned vectors per note — a fixed `bitset<16×128>` for clip playback and a 256-entry stack for MidiPitch. (6) **MIDI-learn lookups** allocated a result vector per CC/note message — now callback-based `forEachByCC`/`forEachByNote` on the audio thread; plus the adjacent race: open input ports live in fixed slots with an atomically published count, so opening a port from the UI can't reallocate under the audio thread. All 1442 tests pass (ASan suite green) |
| **v0.86.13** | **Fix: misbehaving VST3 plugins can no longer take the DAW down from any entry point.** Plugin code ran unguarded everywhere: `create`/`initialize`/`activate` threw C++ exceptions straight into the app (now caught, clean load failure + log); `setState` accepted **arbitrary bytes from project files and the editor pipe with no size cap** (now capped at 64 MB — rejected *before* any plugin access — and exception-guarded, getters too); and the audio-thread **`process()` ignored its result code** — a plugin that starts failing was hammered every block forever (now: first failure logs once, renames the device `[Failed]`, and hard-bypasses it). A true SIGSEGV still can't be caught in-process — that's why editors already run out-of-process; full audio sandboxing is documented as out of scope. All 1441 tests pass (ASan suite green) |
| **v0.86.12** | **Fix: VST3 plugins are now scanned out-of-process — a crashy plugin can no longer kill (or crash-loop) the DAW at startup.** The startup scan dlopened every plugin **in-process** (static initializers + factory queries run at `Module::create`), so one broken plugin crashed the app during scan — and since the cache only existed after a *complete* scan, every later launch crashed identically. `yawn_vst3_host` gains a **`--scan <module>` mode** (enumerate audio-processor classes in the subprocess, JSON to stdout — verified live against Dexed), and `VST3Scanner::scanOutOfProcess` spawns it per module (stdout span-parsed so plugin printfs can't corrupt the parse). A crashing plugin now only marks its own cache entry **failed** (terminal — no retry loop). The **v2 cache is per-module and written incrementally**, so a killed scan *resumes* next launch instead of restarting — and the in-process scan remains only as a fallback when the host binary is missing; legacy v1 caches still load. 5 new scanner tests. All 1440 tests pass (ASan suite green) |
| **v0.86.11** | **Refactor: device displays are a registry, not a 740-line header if-chain.** `DetailPanelWidget.h` carried ~740 lines of `if (nm == "FM Synth") …` branches (12 instruments + 4 audio effects + 1 MIDI) keyed on display-name strings — every new device meant editing a 2,756-line header included by half the UI and recompiling everything downstream. All per-device knowledge (display widget, section→param tables, per-frame updaters, callback wiring) now lives in **`DetailDisplayRegistry.{h,cpp}`**: one builder function + one table row per device, looked up by `name()`/`id()`. The panel's three dispatchers are thin lookups; builders reach its callback members through a single befriended accessor instead of a dozen new public getters. **Adding a device's display is now a one-file change** with zero downstream recompiles, and the header shrank to 2,186 lines. All 1436 tests pass (ASan suite green) |
| **v0.86.10** | **Refactor: the visual-clip state machine is out of the `App` god object.** `App` had accreted ~1,100 lines of visual glue and ~40 per-track fields — session/quantized launch queues, follow actions, arrangement playback tracking, visual-knob automation + macro/LFO evaluation, knob-CC bus versions, stop-counter tracking, video imports, and shader/model/scene asset localization. All of it now lives in **`VisualClipController`** (`src/app/VisualClipController.{h,cpp}`), holding constructor-injected `Project`/`AudioEngine`/`VisualEngine` references and talking back to the shell only through a narrow **`VisualClipHost`** interface (11 one-line methods: toasts, session-grid indicators, dirty marking, selection, project path) — it never touches `App`'s other subsystems. `App` keeps thin forwarders for the ~35 existing call sites (its `App_Visual.cpp` went from **1,122 → 175 lines**), the three per-frame polling blocks in `App::update()` collapsed to one-line calls, and reset paths use accessors instead of poking fields. No behavior change — this clears the way for the remaining god-object splits. All 1436 tests pass (ASan suite green) |
| **v0.86.9** | **CI & build hygiene: pinned dependencies, sanitizer + debug CI jobs, an honest warning gate, and the RT input path under test.** Every release artifact's reproducibility previously depended on the state of four moving branches — **SDL3 tracked `main`** unpinned (and the Linux-only `_deps` cache meant the Windows build re-cloned a *different* `main` every run, so one release could embed two different SDL builds), eigen/stb/minimp3 floated on `master`, and the ONNX Runtime download had no integrity check — all now pinned to known commits, with `URL_HASH` (SHA-256) on the ONNX fetch and Windows FFmpeg pinned to a dated autobuild. The warning gate gets honest: `-Werror=infinite-recursion` was **silently inert on GCC** (Clang-only warning — the Linux CI compiler never checked it), `yawn_vst3_host` (the process that loads third-party plugin UI code) got no warnings at all, and tests marked our own headers `SYSTEM` (suppressing our own warnings in test TUs) — all fixed. New **sanitizers job** (Linux ASan+UBSan over the full suite — the retire-list/SPSC work from v0.86.5 is exactly their bug class, and CI ran none of it) and **debug job** (the `assert()`s in `src/` compiled out under NDEBUG in the Release-only matrix — first job where they fire). The 6 DISABLED PrivateCapture tests are **re-enabled** via the `initHeadlessForTest` seam — the real-time callback's input path finally has executable coverage. All 1436 tests pass (ASan suite green) |
| **v0.86.8** | **Perf: recording and time-stretch allocation moved off the real-time audio thread.** Three guaranteed glitch sources in normal workflows. (1) **Record start** resized the 5-minute recording pool (~115 MB alloc+zero) *in the audio-thread command handler* and grew the MIDI take vectors per note on first takes — new `prepareRecording()` sizes the pool and reserves the vectors on the UI thread when you arm a track and before every record-start orchestration, so the handler never allocates. (2) **Record finalize** memcpy'd the whole take (up to ~115 MB) on the audio thread — the common path is now a **zero-copy swap**: the pool buffer moves to the UI transfer slot with its native stride (`strideFrames`), the clip-buffer copy happens off-thread, and `releaseRecordedAudioBuffer()` round-trips the pool for the next take; MIDI notes/CCs are `std::move`d instead of copied. (3) **Time-stretcher init ran at clip launch / warp-mode change** — WSOLA/Phase-Vocoder/PGHI buffer setup is now split into `preallocate()` (all three algorithms, all 64 tracks, done once at engine init and on sample-rate change) and an allocation-free `init()` at launch; the arrangement stretcher got the same treatment and its `thread_local` gather vector is a preallocated member. New test seams (`initHeadlessForTest`, `setHasInputDeviceForTest`) enable true end-to-end record→finalize tests headlessly. All 1430 tests pass (ASan suite green) |
| **v0.86.7** | **Fix: corrupt project files can no longer crash the app, and a failed save can no longer create one.** Loading was unprotected past the JSON parse: a type-confused field (`"volume": "loud"`) threw straight out of the frame loop and terminated the app, `std::stoi` on file keys (`track:scene` clip keys, macro indices) did the same on non-numeric garbage, and file-supplied counts/indices reached fixed engine arrays unchecked — a 65-track file was an out-of-bounds **write**, and out-of-range clip/zone loop points were out-of-bounds **reads on the audio thread** (release builds; the bounds `assert` compiles out). Every entity (track/scene/clip/mixer) now deserializes inside its own try/catch — one bad entry is logged and skipped, the rest of the project loads; `stoi` is an exception-free `from_chars` parse; track/scene counts clamp to `kMaxTracks`/`kMaxScenes` with a warning; engine accessors are bounds-clamped; and loop points are clamped at the deserializer, in `addZone`, and again on the RT wrap path. Saving: `project.json` is now written via **temp+rename** (a crash or full disk mid-save leaves the old *or* new file intact), short WAV writes / failed OGG `fwrite`s fail the save loudly, and sample references land in the JSON only when the sample write actually succeeded. 11 new hardening tests. All 1423 tests pass (ASan suite green) |
| **v0.86.6** | **Fix: undoing the removal of an Auto Panner restored nothing — and the device catalog is now one table.** The built-in device list was copy-pasted into **7 places** (four Add-Effect menus, two instrument menus, the MIDI-FX menu, the serializer's id factory, the undo system's name factory, and the library classifier), and the copies had silently diverged: `AutoPanner::name()` ("Auto Panner") had no entry in `createAudioEffectByName`, so undo of "Remove Auto Panner" quietly no-opped. There is now a **single `DeviceDescriptor{displayName, id, className, alias, make}` table** per device kind in `util/Factory.h` — every menu iterates it, both factory styles look up in it (by-name accepts menu label, class `name()`, id, or a legacy alias), and the library classifier matches against it — so a device added in one place can never drift out of another again. The factory tests are **data-driven over the tables** (id/`id()`/`name()`/alias round-trips + a pinned Auto Panner regression), turning any future drift into an instant CI failure. All 1412 tests pass (ASan suite green) |
| **v0.86.5** | **Fix: cross-thread object lifetime — one retire-list + atomic-publish mechanism everywhere.** The UI thread was destroying objects while the real-time audio thread was still inside them: swapping an instrument (`setInstrument`) or deleting an effect dropped the old one mid-`process()`, loading a sample reallocated the buffer under running voices (Sampler, Granular, Multisampler, DrumRack pads, Vocoder), and clip-automation vectors dangled on scene insert/delete or "Clear Automation" — all straight use-after-free during ordinary editing. The NAM/convolution RCU trick is now a shared **`util::RtRetireList`** keyed to a new audio-callback **heartbeat** (frees only after a 3-callback grace — exact, and a stalled audio thread can't cause a premature free, unlike the old wall-clock TTLs). Instruments publish via an atomic slot; **effect chains publish immutable snapshots** that the audio thread iterates (with `removeRetired` for delete flows); **sample data is boxed** and atomically swapped, with DrumRack pads / Multisampler voices pinning a `shared_ptr` at trigger (tails outlive any grace); **clip automation is boxed + copy-on-write**, with an `UpdateClipAutomationMsg` so edits land on the next block instead of the next launch — and scene/track deletion now graveyards the destroyed slots' clips too (a hole that predated the automation one). Also removed the dead `MoveAudio/MidiEffectMsg` handlers that double-applied UI-side moves on the audio thread. 25 new tests cover the retire list, chain snapshots, sample swaps, automation boxing and engine lifetime. All 1410 tests pass (ASan suite green) |
| **v0.86.4** | **Subdivide post-FX: `modulate` knob (tinted mosaic vs plain copies).** The subdivide effect tiles `iPrev` into an N×N grid of copies; a new **`modulate`** control (0–1, default 1) chooses whether each cell is tinted by the source's colour at that cell — so the grid of tints approximates the original image — or is a **plain, unaltered copy** (`modulate = 0`), crossfading in between. Also upgraded the tint from luminance-only to **full colour**, so the mosaic now adopts the source's actual hues, not just brightness |
| **v0.86.3** | **Fix: video export now carries macro/LFO modulation (visual *and* audio).** When the export moved onto the main thread (v0.85.6) its `if (!vExport)` gate also skipped `applyMacroMappings()` + `pollVisualKnobAutomation()`, so the rendered video was frozen at base values — clip knob-LFOs advanced (they read the synthetic beat in `tick`) but anything the macros *mapped to* (shader-source / effect-chain params) and automated knobs didn't. The export frame loop now re-applies both per frame. For **audio**, the offline bounce gained a **per-block hook** (`OfflineRenderer` `onBlock`): `applyAudioMacroModulation(beat)` pushes each track's LFO-modulated macro value to its instrument/effect/volume/pan targets before each render block, so the bounced audio modulates like live. And audio-track macros now **evaluate their LFOs** at all (previously raw value — the "Phase 4.6" TODO), so audio macro LFOs breathe their mapped params live too. All 1383 tests pass |
| **v0.86.2** | **Static images as a visual source + no-crop transforms.** Drop a **PNG/JPG/BMP/TGA/GIF** onto a visual track and it loads as an iChannel2 source (no transcode, no save required — referenced in place, cascading down scenes like video). `VisualEngine::setLayerImage` `stbi_load`s it (forced RGBA, so **PNG alpha is preserved**) into a native-size texture, mutually exclusive with video/live/model. The bundled **`image_fit.frag`** aspect-**fits** it with a transparent letterbox and **premultiplied alpha** (transparent images layer cleanly), driven by the image's real size (now fed into `iChannelResolution[2]`, previously hardcoded). New `VisualClip.imagePath` is cloned + serialized. Also: the **rotate** / **scale** post-FX and the image's `scale` gained a **`fit`** knob — rotate auto-shrinks so a turned frame never crops its corners/edges (default on), and scale offers zoom (crop) vs resize (stay fully visible). All 1383 tests pass |
| **v0.86.1** | **Three new shader sources: text crawl, disco ball, hypercube morph.** **`28_text_crawl`** — a Star Wars-style perspective crawl of the clip's text (right-click → Set Text), wrapping the single-line text channel into stacked rows that recede to a centre vanishing point and loop; knobs for speed / lineWidth / horizon / density / spread / colour; transparent outside the glyphs so it layers over a starfield. **`29_disco_ball`** — a raymarched spinning mirror sphere with lat/long mirror tiles (dark grout), per-tile flash + specular catch, over revolving coloured light glints; premultiplied-alpha so the room is transparent and the ball layers over other visuals; knobs speed / facets / sparkle / hue / **scale** (ball size). **`30_hypercube_morph`** — a 4D wireframe (32 edges, projected 4D→3D→2D) that **morphs point→square→cube→tesseract** via `morph`, with `hue`, `speed`, `thickness` and **scale**. All auto-discovered by ShaderLibrary. |
| **v0.86.0** | **New shader: `27_hypno_spiral` — a 60's-style rotating hypnotic spiral.** A two-tone logarithmic swirl in the Sources library with `@range` knobs: **arms** (2–16, rounded to an integer for a seamless wrap), **color1** / **color2** (hue 0–1 — the shader param system is float-only, so colours are hue knobs), **centerRadius** (0–0.5, the centre slowly orbits a circle of this radius; 0 = pinned), **waviness** (0–1, straight→wobbly arms) and **speed** (−20…20, rotation). Auto-discovered by ShaderLibrary — no manifest change. |
| **v0.85.9** | **Fix: text-field editing (Set Text horizontal scroll + typing a tempo).** Two input bugs. (1) `FwTextInput` (the Set Text dialog, browser search) drew text at a fixed left edge and clipped it, so once the caret passed the field width it vanished and long text was un-editable — the arrow keys *were* moving the cursor, you just couldn't see it. The painter now **scrolls horizontally to keep the caret in view** (persisted offset), so long strings are editable and Left/Right/Home/End work. (2) Double-clicking the transport **BPM / time-signature** let you Backspace but not type digits: the field's gesture SM enters edit mode outside the mousedown handler's one-shot `SDL_StartTextInput`, so SDL never emitted `TEXT_INPUT` for the digits (Backspace works as a KEY_DOWN). Now reconciled **every frame** — text input is turned on whenever the transport is editing — so digits flow regardless of how edit was entered. All 1383 tests pass |
| **v0.85.8** | **Session→arrangement capture is now a `▸ARR` transport button.** Promoted out of the Tools menu onto the transport, sitting right of `AUTO` so the three arming toggles read as a set (`●REC` clip-record / `AUTO` automation / `▸ARR` arrangement). Click to arm — it lights red in the same language as the other arm toggles — play and trigger clips, click again to disarm and get the "Bounce N captured clips?" confirm. The button doubles as the armed indicator, so the separate `● REC▸ARR` badge by the position readout is gone, and the Tools-menu entry is removed. Pure toggle wired to `toggleArrangementRecord`, lit state synced back via `setArrangementRecArmed` (so load/undo stay correct). All 1383 tests pass |
| **v0.85.7** | **Video export: stream frames into ffmpeg's stdin (no PNG sequence).** The v0.85.6 export rendered every frame to a PNG on disk, then ran ffmpeg as a separate pass over the sequence — slow for long timelines (a 105-bar arrangement = 6240 PNGs written then re-read). New cross-platform `FfmpegPipe` (POSIX `pipe`+`fork` / Win32 `CreatePipe`+`CreateProcess`) wires ffmpeg's **stdin to a raw-RGBA pipe**: each rendered frame is written straight to ffmpeg (`-f rawvideo -pixel_format rgba -i -` + the bounced WAV), so **encoding overlaps the render** in a single pass with **no PNG disk I/O**. The pipe back-pressures to the encoder's rate, so total time ≈ the slower of render-vs-encode instead of their sum; the final phase is just closing stdin and draining ffmpeg's tail (on a worker). Cancel closes the pipe and removes the partial file. All 1383 tests pass |
| **v0.85.6** | **Video export: responsive, with a real progress bar — and no more GL corruption.** The v0.85.3–4 export blocked the main thread (OS "not responding"), and a first attempt at rendering on a *worker* thread corrupted the visual GL context (post-export `FBO incomplete (0x0000)` + shader-compile failures — a GL context can't be safely driven off-thread here). The export is now a **frame-driven state machine**: the GL frame render stays on the **main thread but is sliced to ~12 ms per UI frame** (the window stays live, the bar animates, Esc/Cancel aborts instantly), while the **non-GL phases run on a worker thread** — the audio bounce (0–10%) and the ffmpeg encode (now `-preset veryfast`, 88–95%). It reuses the audio export dialog's render-mode **progress bar**, **closes itself** on completion with a toast, and restores transport + arrangement-active state. Cancellation is wired through all three phases. (Follow-up for big timelines: pipe frames straight to ffmpeg instead of writing a PNG sequence.) All 1383 tests pass |
| **v0.85.5** | **Fix: record-session-into-arrangement dropped tracks whose clip looped on a single scene, and trimmed the lead-in bar.** Two bugs in the bounce. (1) On disarm, still-open take intervals were closed at the **live transport position** — which is **0 after you press Stop** — so any interval still open got closed *backwards* (`stop < start`) and silently dropped as "too-short". Tracks whose clip just looped one scene (no follow-action scene change) had **only** that one open interval, so they vanished entirely; scene-changing tracks survived on their earlier closed intervals. Disarm now closes open intervals at the **recording high-water beat** (`m_arrRecLastBeat`, last beat seen while playing+armed), so the common **Stop → toggle-off-to-commit** workflow captures every track — and every track keeps its **final** segment too. (2) The bounce is now **normalized to the arrangement origin**: the earliest captured downbeat lands on bar 1 (relative timing between tracks preserved), trimming the silent lead-in bar from the NextBar launch quantize. All 1383 tests pass |
| **v0.85.4** | **Fix: video export no longer triggers the OS "not responding" dialog.** The v0.85.3 render blocks the main thread, so the window manager's watchdog popped a "com.yawn.daw is not responding" prompt. The render now **pumps SDL events** every few frames (and around the audio bounce + ffmpeg encode) — which answers the WM ping — and shows progress in the **title bar** (restored when done). The window still doesn't repaint mid-render (a fully responsive chunked/threaded export is the proper follow-up), but it no longer looks crashed; a long render will finish if left alone |
| **v0.85.3** | **Video export (2/2) — File → "Export Video (mp4)…".** Renders the whole arrangement to an mp4: a native save dialog, then an offline bounce — audio via `OfflineRenderer` → WAV, and the visuals frame-by-frame at **640×360 / 30 fps** by stepping the transport, driving the arrangement visual launches, rendering each composite via the new offline `tick()`, reading it back, and writing a PNG sequence; then ffmpeg encodes **H.264 + AAC** and muxes (reuses the importer's process spawn; needs ffmpeg installed). All arrangement tracks are forced active for the bounce and restored after. **v1 caveats:** the render is **blocking** (the UI is unresponsive while it works — progress goes to `yawn.log`, a toast lands when done), and it's **build-verified only** — the live GL+ffmpeg pipeline needs real-world testing. Faster-than-real-time is deterministic thanks to the v0.85.2 synthetic-time work. All 1383 tests pass |
| **v0.85.2** | **Video export — offline-render infrastructure (1/2).** Groundwork for rendering the arrangement to an mp4 (no user-facing action yet). `VisualEngine` gains an **offline render mode**: `tick()` renders into the FBOs even with the output window hidden, skips the on-screen blit, and drives the post-FX chain's `iTime` (the one wall-clock time source that wasn't transport-derived) from the synthetic transport time — so a faster/slower-than-real-time render is reproducible (arrangement clips were already transport-driven and deterministic). Plus `readComposite()` to read the last composited 640×360 frame back as top-down RGBA8 (`glReadPixels` flipped). The export driver + ffmpeg muxing + UI land next. Build-verified |
| **v0.85.1** | **Record session → arrangement: visual capture + armed indicator.** The session→arrangement recorder now captures **visual** tracks too (using the per-track active session visual-clip scene the app already tracks, including follow-action changes), so a triggered shader/video/scene performance bounces to the timeline alongside audio/MIDI. The transport bar shows a red **● REC▸ARR** badge while armed, so a long take isn't a surprise. Completes the record feature for all three track types. All 1383 tests pass |
| **v0.85.0** | **Record session → arrangement (audio + MIDI).** Ableton-style performance capture: arm **Tools → "Record Session into Arrangement"**, play and trigger session clips live, then pick it again to stop and confirm **"Bounce N captured clips to the arrangement?"**. While armed, each frame polls the *actually-playing* session clip per track (so follow-actions and scene launches are captured for free) and accumulates per-track take intervals in timeline beats. On commit it snaps interval edges to the nearest beat (launches are quantized, so this recovers the boundary), **punch-overwrites** the recorded range on each captured track, and synthesizes arrangement clips referencing the session content (audio shares the buffer + inherits looping; MIDI clones the clip), flips those tracks to arrangement-active, and re-syncs the engine. Capture-then-commit, so a take is discardable. (Next: **visual-track capture** and a visible armed indicator.) Manual placement (drag / Send to Arrangement) still available. All 1383 tests pass |
| **v0.84.9** | **Fix: Space double-launched session clips on arrangement tracks.** Pressing **Space** to play launched each track's session default clip *unconditionally* — including arrangement-active tracks, which are already played by the arrangement engine. So an arrangement-active MIDI track *also* fired its looping session clip, which loops forever independent of the arrangement clip's extent — the drums kept playing past the (4-bar, looped) MIDI clip's end and survived stop/restart, while audio tracks (empty session slots) stopped correctly. Space now **skips session-clip launch for arrangement-active tracks**, matching the arrangement Play button (which was already correct). Session-mode tracks are unaffected. All 1383 tests pass |
| **v0.84.8** | **⌥-drag to stretch an arrangement clip.** Holding **Alt/⌥ while dragging a clip's left or right edge** turns Stretch on for that clip and resizes it, so the content stretches to fill instead of looping/trimming — the quick gesture alongside the menu's "Stretch to fit" toggle. Gated to the resize edges (⌥ on the clip body remains the cross-panel drag-to-instrument gesture); the waveform/note preview and L/S badge update live, and the engine re-syncs on release. Completes the loop/stretch effort. All 1383 tests pass |
| **v0.84.7** | **Arrangement audio stretch (live time-stretcher) — stretch now complete for all clip types.** A stretched audio clip's source is run through a per-channel `TimeStretcher` (WSOLA, pitch-preserving) on the audio thread so it fills its slot exactly — slow a 2-bar loop down to 4 bars and the pitch holds. Each arrangement track lazily allocates its stretchers on first use (one-time, like the session ClipEngine), then `fillStretch` produces output in chunks (gather source → `process` → drain) read one frame per sample; `ratio = contentFrames / slotFrames`, re-primed on clip entry (and from the right source position when the playhead enters mid-clip). Clips shorter than the analysis window fall through to the direct read. The "Stretch to fit" menu now covers audio (joining MIDI + video), and the arrangement waveform fills the clip with one scaled draw when stretched (vs tiling for loop / blank tail for one-shot). New test: a 2-beat source stretched to a 4-beat slot fills it, where a one-shot is silent. All 1383 tests pass. (Remaining: the ⌥-drag stretch shortcut.) |
| **v0.84.6** | **Arrangement stretch — MIDI + video.** "Stretch to fit" (overrides Loop) now time-stretches **MIDI** clips as well as video: the content's notes/CCs are mapped onto the slot so a clip stretched to 2× fires its notes at half speed (non-destructive — the source clip is untouched). The arrangement engine's MIDI scan was generalised to a `base`/`scale` mapping (`timeline = base + (contentBeat − offset) × scale`); loop/one-shot use scale 1, stretch uses slot÷content. The note preview scales to match (stretch fills the clip, loop tiles, one-shot draws once). The clip menu's **Stretch to fit** toggle now appears for MIDI and video clips. New test pins that stretching doubles a note's frame offset. (**Audio** stretch — the live time-stretcher in arrangement playback — and the ⌥-drag shortcut are the remaining pieces.) All 1382 tests pass |
| **v0.84.5** | **Fix hung MIDI note past a looping clip's end.** A looped arrangement MIDI clip whose length isn't a whole multiple of its loop length cut its final iteration short at the clip end — and a note straddling that cut got a note-on with no note-off, so it sounded forever past the clip (reported on the green MIDI track). The per-iteration note-off pass now force-releases any note still sounding across a **hard boundary** (loop end *or* clip end), at that boundary, generalising the earlier loop-end-only handling. Also covers the long-standing one-shot case where a note overrunning the clip end never released. New test asserts note-on/off counts balance across a clip whose end bisects a note. All 1381 tests pass |
| **v0.84.4** | **MIDI loop on extend (arrangement).** Completes the loop model for the last clip type: a looping MIDI clip extended past its content now **repeats** its notes/CCs (tiling the loop region from `offsetBeats` to the clip's content end) instead of going silent. The arrangement engine's per-buffer note scan was refactored into an `emitWindow` pass driven once per loop iteration the buffer touches (almost always one — buffers are a tiny fraction of a beat); note ends are clamped to the loop boundary so nothing hangs across it, and the off-before-on two-pass ordering is preserved. The arrangement view **tiles the MIDI note preview** to match (and one-shots draw once with a blank tail). One-shot (Loop off) MIDI still plays once and is silent past its end. Three new tests pin audio loop-wrap, audio one-shot silence, and MIDI loop-repeat. All 1380 tests pass. (Next: **stretch** — ⌥-drag + "Stretch to fit", with the live audio time-stretcher.) |
| **v0.84.3** | **Arrangement clips: Loop vs silence on extend (audio + video).** Replaced v0.84.1's visual length-mode with a unified **`loop` / `stretch`** model on every arrangement clip. With **Loop on**, extending a clip repeats its content from the trim-in point (`offsetBeats`) to the source end; with **Loop off**, extending adds **silence** (audio) / **holds the last frame** (video) — fixing the old behaviour where audio went silent past the source while the *waveform* stretched to fill the clip and looked like it was playing. The arrangement **waveform now tiles** when looping and shows **only the real content** (blank tail) when not, so the drawing is honest about what you'll hear. A **loop/stretch badge** (L / 1 / S) sits on each clip, and the clip menu has a **Loop** toggle (all types) plus **Stretch to fit** (video). "Send to Arrangement" inherits loop from the session clip (one-shots don't loop) and stretch from a synced video. Audio loops by wrapping the play position; **video** loops/holds/stretches via the same per-clip flags. (**MIDI** loop-on-extend lands in the next commit; **stretch for audio/MIDI** is the step after.) All 1377 tests pass |
| **v0.84.2** | **Discoverable arrangement clip trim/extend handles.** Resizing arrangement clips already worked, but the grab zones were invisible 6 px edges with no cursor feedback, so it wasn't obvious you could do it. Now hovering (or selecting) a clip shows **grab handles on both edges** — left = trim the start (`offsetBeats`), right = trim/extend the end (`lengthBeats`) — the edge under the cursor brightens, and the pointer switches to the **horizontal-resize (↔) cursor** over an edge or mid-drag. Extending a visual clip loops it (with the default **Loop** length mode from v0.84.1), or stretches/plays-once per the clip's mode. (Audio/MIDI clips still play their source once and go silent past its end — looping them on extend is a separate engine change.) All 1377 tests pass |
| **v0.84.1** | **Arrangement visual clips — foundations** (step 1 toward session→arrangement recording + video export). Visual clips on the arrangement timeline gain a **per-clip length mode** — **Loop** (native rate, the historical default), **Stretch to fit** (time-stretch to fill the slot, tracking tempo), or **Play once** (play through, hold the last frame) — set from the clip's right-click **Length Mode** menu and wired into the transport-driven video frame path. The timeline now **renders the video thumbnail** behind the clip (like the session grid) instead of a bare glyph, with a small **L/S/1 length-mode tag**; **left-trimming** a clip (`offsetBeats`) seeks into the source. **"Send to Arrangement"** picks the mode from the session clip's Tempo Sync (synced → Stretch, else Loop). Plus a convenience long-requested: **Clear Arrangement (Track)** and **Clear Arrangement (All Tracks…)** on the track menu — empty a lane (or all of them) and return tracks to session control, undoable, with a confirm on all-tracks. Length mode/tag only show for file-video clips (shaders/scenes run on the transport clock regardless). All 1377 tests pass |
| **v0.84.0** | **Tempo-synced visual clips.** Visual clips (videos, shaders, scenes) can now follow the project tempo like audio/MIDI do, instead of free-running on wall-clock. Each visual clip has a unified musical **length in bars** (`VisualClip::lengthBeats`); when **Tempo Sync** is on the clip locks to the transport: a **video** is stretched to play its trimmed range over that length and loops, so it speeds up/slows down with BPM; a **shader / Lua scene / model animation** gets a beat-driven `iTime` referenced to 120 BPM — unchanged at 120, scaling linearly with tempo, and holding when the transport stops (all three run off the same `preWall` clock, so one change covers them). **Imported videos auto-detect** their musical length from the source duration (ffprobe → rounded to the nearest beat at the current BPM) and turn sync **on** by default, so they track tempo out of the box; flip Tempo Sync off for native-rate playback. Sync is **opt-in** for shaders/scenes (default off, so existing projects look identical). A synced visual-only clip launched while the transport is stopped now starts the transport (audio/MIDI launches already do), so synced visuals never sit frozen. **Session slots** draw the **"N bars"** badge for visual clips like audio/MIDI clips — accent-coloured when synced, dimmed when free-running — and the clip menu gains a **Tempo Sync** toggle (Clip Length live-updates a playing synced clip). Legacy `videoLoopBars>0` clips keep working (treated as tempo-locked). All 1377 tests pass |
| **v0.83.7** | **Fix inverted warp direction.** A warped clip (any mode — Repitch / Tones / Beats / Texture / PGHI / Auto) sped up when the transport BPM was *lowered* and slowed down when it was *raised* — backwards. `Clip::warpSpeedRatio` returned `originalBPM/targetBPM`, but every consumer treats that value as source-frames advanced per output frame (`pos += ratio` in the resampling path; the time-stretcher's `setSpeedRatio` in Tones/Beats), where >1 = faster — so a higher project tempo produced a *smaller* ratio. Flipped to `targetBPM/originalBPM`, which also makes it agree with the warp-marker path (`computeLocalSpeedRatio`, `srcSamples/dstSamples`) that was already correct. A clip captured at its `originalBPM` now tracks the project tempo the right way: raise the BPM, it plays faster (pitch held constant in Tones). Note: existing projects with warped clips will now warp in the correct direction (the old direction was simply inverted) |
| **v0.83.6** | **Neural Amp becomes a full amp channel.** The NAM device wrapped the raw model with only In / Out / Mix and exposed *no* knobs (its custom panel replaced the knob grid). It now renders as a proper amp strip — the model panel (filename / Load / Clear / Lite) on the left with the built-in controls grouped to its right — by reusing the same `GroupedKnobBody` the instruments use. New built-in DSP around the mono model core: (1) a **Noise Gate** on the input (peak-follower, 2 ms open / 50 ms close, threshold knob, bypassed at the −100 dB floor) to kill amp hiss/hum between notes; (2) a 3-band **Tone stack** on the output — **Bass** (200 Hz shelf) / **Mid** (800 Hz peak) / **Treble** (3.2 kHz shelf), ±12 dB, same topology as AmpSimulator; (3) a **Normalize** toggle that reads the model's embedded loudness metadata (`HasLoudness`/`GetLoudness`) and levels output toward a −18 dB reference so swapping captures doesn't jump in volume. The gate + tone stack also run in the no-model/NAM-less fallback so the knobs are never inert. Plus a **sample-rate-mismatch warning**: NAM models bake in their training rate, and running inference at a different host rate subtly changes tone — we don't resample (neither does the stock plugin) but now log it and show an amber `⚠ trained @ N Hz` on the device. That closes the oversampling/quality sweep — NAM is a fixed-rate model engine, so it's anti-aliased by design rather than by oversampling |
| **v0.83.5** | **Anti-aliasing pass — the saturators.** **AmpSimulator** (tube saturation), **TapeEmulation** (tape saturation) and **Rotary** (pre-rotor drive) each gain an **OS 2×** toggle (off by default), wrapping their `tanh` waveshaper in the shared `Oversampler2x` so the harmonics it generates stay below Nyquist instead of folding back as harsh fizz. Each effect's surrounding linear stages — tone stacks, cab/delay lines, crossover, Doppler/amplitude modulation — stay at the host rate. That completes the oversampling sweep of the built-in nonlinear DSP (master clipper, DrumSynth drives, Distortion, Bitcrusher, and now these three); the only remaining nonlinear processor is the NAM neural amp, which is a separate model engine |
| **v0.83.4** | **Anti-aliasing pass — Distortion & Bitcrusher.** Both gain an **OS 2×** toggle (off by default — sound is identical until flipped on; ~2× CPU for that effect + ~0.3 ms latency while on). **Distortion** wraps its waveshaper (`shape()` — soft/hard-clip, tube, foldback) in the shared `Oversampler2x` so the harmonics it generates stay below Nyquist instead of folding back; biggest win on hard-clip/foldback. **Bitcrusher** runs its sample-and-hold + bit-quantize core at 2× — the quantizer is a memoryless nonlinearity whose harmonics alias, so this cleans the harsh top-end fizz, while the intentional in-band sample-rate-reduction "crunch" is preserved. (The linear effects — EQ, spline EQ, auto-filter, wah, phaser, chorus, delays — were left alone; they create no new harmonics, so nothing to anti-alias.) |
| **v0.83.3** | **Anti-aliasing pass, cont.** Extracted the half-band 2× oversampling from DrumSynth into a reusable `audio/Oversampling.h` — `Downsampler2x` (for sources that render at 2×), `Upsampler2x`, and `Oversampler2x` (upsample → apply a memoryless nonlinearity → downsample, for effects/clippers); 32-tap windowed-sinc half-band FIR, unit-tested for unity gain, reconstruction, and ≥20 dB alias suppression. The **master soft-clipper** now runs through `Oversampler2x` so its saturation can't alias, with a new **Oversample Master Clip** toggle in Preferences → Audio (on by default; ~0.3 ms latency, hence the switch). Linear effects (EQ, the spline EQ, auto-filter, wah, phaser, chorus, delays) were audited and correctly left alone — they create no new harmonics, so oversampling them would be pure cost. Next: the genuinely nonlinear effects (Distortion, Bitcrusher, the tanh saturators) |
| **v0.83.2** | **Audio quality — anti-aliasing pass.** (1) **Softer master clipper.** The always-on master safety soft-clip engaged at 0.95 with a near-brickwall 0.05 knee, so any hot multi-track mix got harsh high-order harmonics (which also alias). Lowered to 0.7 with a wide 0.3 knee: effectively transparent up to ~-1.5 dB, warming rather than crunching the peaks above it. (2) **DrumSynth 2× oversampling.** Each drum's tanh `drive` saturator generates harmonics above Nyquist that fold back as inharmonic "fizz" above the drum's pitch. A new **OS 2x** toggle (on by default) renders the whole kit at twice the host rate — doubling the rate *and* the frame count keeps every rate-derived envelope/oscillator increment musically identical — then a 32-tap half-band FIR decimator removes everything above the host Nyquist before dropping every other sample, so the drive harmonics can't alias. ~2× CPU for the drum synth while on; a panic (All Notes Off) flushes the decimator so silence stays immediate. First step of a broader oversampling pass (the master clipper and distortion-type effects are next) |
| **v0.83.1** | **Export fixes.** (1) **Exporting at a sample rate other than the audio interface's no longer changes speed/pitch** — the offline bounce rendered at the device rate but wrote the *selected* rate into the WAV header, so a 48 kHz render tagged 96 kHz played back ~2× fast. The render stays at the native rate (so clip playback, synths and effects all stay correct — instruments are rate-configured at `init()` and don't re-rate cleanly), and the finished buffer is resampled to the chosen rate so the header and samples agree. (2) **Exporting from the Session view is no longer silent** — export is an arrangement bounce, but Session view deactivates the arrangement tracks (the engine plays the clip launcher instead); the bounce now forces arrangement playback on for every track with timeline clips and restores each track's state afterward |
| **v0.83.0** | **Session-view launch/drag fixes + a clip-edit crash fix.** (1) **Dropped files land where you drop them** — on Linux SDL's `DROP_FILE` reports `(0,0)`, so files fell into the *selected* track's default slot instead of under the cursor; the drop point now comes from `DROP_POSITION`/the OS mouse state, and dropping several videos cascades down successive scenes. (2) **Scene-launch A/V sync** — video/shaders launched immediately while audio/MIDI honoured the slot's launch-quantize, so visuals led the audio by up to a bar; visual launches now defer to the same bar/beat boundary, including the launch-from-stopped case (the transport spins up and the clips land on the next bar together). (3) **Follow actions** — audio-clip follow actions never fired (the bar-counter was only reachable via a code path the session clip engine doesn't use), and a follow-action clip switch played one extra bar (it re-quantized the next clip to NextBar after the action already fired on the bar); both fixed. (4) **Crash fix** — inserting/deleting/duplicating a scene while a clip with recorded automation was playing could dangle a `&slot->clipAutomation` pointer the audio thread dereferences every buffer (SIGSEGV); structural scene edits now stop clips first. (5) **Arrangement**: a return-to-start (`\|◀`) button, audio↔MIDI/visual type-safe clip dragging (a clip can't be dropped on a track of another kind), and edge auto-scroll while dragging a clip past the viewport. (6) **Visual clips** gain a **Clear Shader** menu item (mirrors Clear Live Input / Clear Models), and the shader file picker remembers its last folder across sessions |
| **v0.82.0** | **ASIO support on Windows** — the path to low-latency audio on interfaces with vendor ASIO drivers (Scarlett, etc.): ~5-10 ms round trip vs 30 ms+ through WASAPI shared mode. PortAudio is now built with its ASIO host (the Steinberg ASIO SDK is fetched at build time and not redistributed; *ASIO is a trademark and software of Steinberg Media Technologies GmbH* — see NOTICES.md). The Preferences device pickers tag each entry with its host API when devices span several ("Focusrite USB ASIO **[ASIO]**" vs "Speakers **[Windows WASAPI]**") so picking the low-latency entry is deliberate, and the stream-open log records which API is actually in use — the first question of any latency report. **To use:** Preferences → set input *and* output to your interface's [ASIO] entry (same device for both also avoids clock-drift) |
| **v0.81.1** | **Consistent panel splitters.** The piano-roll and detail-panel resize strips were functionally full-width but *looked* like tiny center-dot grips — no resize cursor, no hover highlight — so resizing felt like it only worked on the dots. Both now match the tracks↔mixer divider: NS-resize cursor + hover highlight across the full width, accent color while dragging, and a full-width groove line (hover is computed centrally in the app's mouse handler — fw2 move dispatch only reaches the child under the pointer, so panel-local tracking would go stale). Also fixes the layout side: **ContentGrid kept its rows as ratios**, so growing a bottom panel (or opening the piano roll, or resizing the window) rescaled the mixer row proportionally — dragging the lower splitter visibly resized the upper panes. The mixer row and right column now keep their pixel size, with the session/arrangement pane absorbing all elastic space |
| **v0.81.0** | **OpenGL 3.1 support** — the GL floor drops from 3.3 to 3.1, so YAWN now runs on Windows Sandy Bridge-era Intel drivers (the v0.80.3 reporter's machine: driver offers 3.1) and, looking ahead to ARM, on Raspberry Pi 4/5 (Mesa v3d tops out at desktop GL 3.1). New `ui/GlCaps.h` carries the real context's version + extension flags; on a < 3.3 context the engine-injected shader preambles switch from GLSL 3.30 to **GLSL 1.40**, `layout(location)` qualifiers become `glBindAttribLocation` pre-link bindings, texture swizzle falls back to ARB ext or exact CPU expansion (font atlas, layer text, audio texture), and M3D GPU instancing falls back to the per-instance uniform path (same image, one draw per instance). All 52 bundled shaders are version-agnostic and 1.40-clean. Verified on a real 4.6 context plus simulated 3.1 with and without the ARB extensions (Mesa overrides): all shaders compile, zero GL errors, ASan clean |
| **v0.80.3** | Fix **startup failure on Windows machines with weak/missing GPU drivers** ("Could not create GL context: The operation completed successfully" — SDL relaying `GetLastError()==0` when the driver rejects our strict request without a reason). `Window::create` now runs a **GL context fallback chain** — 3.3 core + 24-bit depth → 3.3 core + default framebuffer → driver-default legacy request verified ≥3.3 via glad (a legacy request can "succeed" with GL 1.1 on Microsoft Basic Display Adapter / Remote Desktop, which previously crashed on the first missing entry point) — recreating the window per attempt since pixel formats bind to the window on Windows. Dropped the 8-bit stencil demand (nothing uses stencil; clipping is scissor-based — it was pure rejection risk). On success the log now records **GL_VENDOR / GL_RENDERER / GL_VERSION** for diagnosable bug reports; on total failure the user gets a **message box** explaining the OpenGL 3.3 requirement and pointing at GPU driver installation instead of a silent exit. The visual-output shared context inherits the winning configuration instead of re-demanding 3.3 core |
| **v0.80.2** | **Split the 9,719-line App.cpp into nine topical translation units** (events, lifecycle, frame, widget tree, track/clip menus, visual glue, visual menus, loading, project ops) — the largest is now ~1,900 lines. Purely mechanical: the file was already contiguously ordered by topic, so every function moved wholesale with its comments and file-local helpers; `App.h` untouched, no code changed. Verified by exact line-partition accounting, a clean link (no undefined/duplicate symbols), the full test suite, and an ASan app run. Bonus: the app target now compiles these TUs in parallel |
| **v0.80.1** | **Real-time-safety + reliability pass** from a full-codebase review. (1) The arrangement-clip UI→audio handoff took a **mutex inside the audio callback** (priority-inversion → potential dropout) and freed the previous clip list on the audio thread — replaced with a lock-free per-track mailbox + retire stack (same RCU-lite idiom as NAM / Conv Reverb; all alloc/free now UI-thread-only, verified with a 2-thread ASan stress test). (2) Convolution Reverb allocated two temp vectors **per audio block** — now `init()`-sized scratch members. (3) **NeuralAmp load failures are visible**: the device panel shows ✗ + reason in red instead of claiming the rejected file is loaded; `modelPath()` / project serialization always describe the model actually processing, and a project whose .nam is missing on this machine keeps its reference (self-heals when the file returns). (4) Device-strip UI (DetailPanel / DeviceWidget / GroupedKnobBody) converted to `unique_ptr` ownership — retires the manual new/delete bug class behind two past UAFs, and fixes panel-destruction + display-reconfigure leaks. (5) `Biquad::computeStereo` dedupes 19 copy-pasted L/R filter designs (halves coefficient math — EnvelopeFollower designs per sample). Plus: SceneScript tests no longer race on a shared temp file under parallel ctest |
| **v0.80.0** | **Auto Panner** audio effect — an LFO sweeps the stereo image left↔right. Amplitude/balance model (cuts the channel opposite the direction of travel), so it's transparent at Depth 0 and at the LFO centre, pans a mono source across the field, and preserves stereo content otherwise. Free-running (Hz) or tempo-synced (2 bar … 1/16, phase-locked to the transport beat while playing); Sine / Triangle / Square / Saw / Sample-&-Hold waveforms, Depth, Phase offset, and wet/dry Mix, with a short gain smoother to declick the hard-edged shapes |
| **v0.79.3** | Fix the **track-header right-click menu** (add effect / instrument) breaking after loading a project. Two causes: (1) loading a project set the saved view-mode *flag* but never switched the UI, so an Arrangement-saved project opened showing the Session view — the menu is gated on the active view, so it silently bailed; loading now calls `switchToView()` to reconcile the UI with the restored flag. (2) The track menu existed only on the **Session-view** headers, so working in Arrangement view meant flipping back to Session just to add a device — the Arrangement track headers now expose the same right-click menu. Also reverts v0.79.2's render-skip (it was a red herring for this bug; the VJ-output stall fix is deferred) |
| **v0.79.2** | Keep the visual (VJ) output smooth when YAWN's main window is **minimized or fully covered** — `render()` now skips the main window's vsync-throttled swap in those states and drives only the visual output, instead of the throttled main swap dragging the output's frame rate down. Keyed on visibility only, so an unfocused-but-visible window still repaints (context menus etc. keep working) |
| **v0.79.1** | Fix a **use-after-free → heap corruption** behind random crashes (record→playback aborts, a Lua scene-script crash, a GL buffer-swap crash). Rebuilding the audio-effect strip (`setAudioClip`) freed the device display panels but kept stale per-frame updater lambdas pointing at them, so the next render wrote into freed memory. Now clears the updaters on teardown (matching the instrument/MIDI paths). Found with AddressSanitizer |
| **v0.79.0** | **Glitch audio effects** — Beat Repeat (tempo-synced grid stutter), Buffer Repeat (free ms stutter/freeze), Resampler (varispeed down/up-sampling), Clock Drift (wandering-clock wow), and CD Error (random skip/stutter/mute). Adds a per-block `setTempo` push to effects (which also fixes Delay/Ping-Pong tempo-sync that was stuck at 120 BPM). Plus: the crash handler is now async-signal-safe — a crash exits cleanly with a `yawn.log` trace instead of hanging |
| **v0.78.1** | Fix track-level glitch shaders (datamosh / bitcrush) doing nothing — `datamosh` redeclared the engine's built-in `iFeedback` uniform, so as a per-track effect-chain pass it failed to compile, and a single failed pass silently drops the *whole* chain. `iFeedback` is now a built-in in both shader preambles (don't redeclare engine uniforms); track-level datamosh/bitcrush now glitch video and any clip |
| **v0.78.0** | **Custom numeric entry** for preset menus — Video Rate, Video Loop, Clip Length, Animation Speed, LFO Rate/Depth, and Video Trim now each offer a **"Custom…"** option to type any value (pre-filled with the current one, clamped to a sensible range), not just the fixed presets |
| **v0.77.2** | Fix a **heap use-after-free** that corrupted the heap and hung the app ("corrupted double linked list") — opening a context-menu submenu (e.g. a clip's Video Rate) wrote through a `Level&` reference invalidated by a `std::vector` reallocation; found with AddressSanitizer. Also: the video scaler now uses the clip's native source size (out-of-bounds reads on non-640×360 video), plus live-monitoring latency logging and a warning when input/output are different audio devices (independent clocks → latency drift) |
| **v0.77.1** | Default to **48 kHz** — a 44.1 kHz engine default forced an OS-level 44.1↔48 resample on modern systems (PipeWire/CoreAudio graphs and most USB interfaces run at 48 k, as do NAM captures), adding live-monitoring latency + CPU that drifted upward under load (heard as NAM latency growing over time / per added instance). The engine still re-queries the device's actual rate on open, so 44.1 k-only devices adapt. Plus FTZ/DAZ denormal flushing on the audio thread (standard hygiene) |
| **v0.77.0** | Neural Amp Modeler **A2** support — pinned `NeuralAmpModelerCore` to v0.5.3 (locks in the A2 architecture; A1 captures still load and run unchanged, A2-shaped WaveNet models route to the optimised `A2FastModel` via `NAM_ENABLE_A2_FAST`). Added a **"Lite"** toggle to the Neural Amp device that drives `nam::SlimmableModel::SetSlimmableSize` (0 = lite … 1 = full) on slimmable A2 models for a substantial CPU saving; greyed out / inert on non-slimmable (all A1) captures |
| **v0.76.4** | Visual clips honour per-column launch — launching a visual clip now updates the session grid's play-state and clears the previously-active clip in that column (the play-icon path had its own stale copy that no-op'd visual stop). Added a **Stop All Clips** button (session corner) that stops every audio/MIDI/visual clip and wipes each track's launch memory so the next Play starts nothing. Plus a ModelLibrary test-flake fix (per-case temp dir) |
| **v0.76.3** | Glitch / malfunction visual pack — datamosh / dropout / bitcrush post-FX, a `26_glitch_testcard` generative scene, and `glitch_stutter` + `note_glitch_ghosts` scene scripts (kick / beat / note-reactive) |
| **v0.76.2** | Post-FX effects preserve layer alpha (pass-through) — HSV / invert / vignette / etc. chained onto a clip no longer blank the lower tracks; rotate / scale leave out-of-bounds regions transparent |
| **v0.76.1** | Piano-roll default note-velocity control — toolbar Vel control with follow-last + persistence; new-note default raised from ~50% to 100 |
| **v0.76.0** | 3D scene overhaul — multi-model scenes, per-instance attributes + camera, MIDI-reactive scenes, PBR-lite materials & lighting, GPU instancing, animation UI, a model-library browser with live thumbnails, and bundled example scenes |
| **v0.75.1** | Fix: session clips now start in phase with the transport on Play — pressing Play launched the default clips on their NextBar quantize grid (deferred behind a stale boundary check), so the audio came in late and trailed the playhead by a constant offset; now they start immediately at the downbeat. Also: README slimmed and split into `docs/` (this CHANGELOG added) |
| **v0.75.0** | Self-contained releases — the Demucs model + ONNX Runtime are now bundled in the release package, so stem separation works offline out of the box (the repo stays lean; the model is fetched at CI build time, never committed) |
| **v0.74.1** | Stem-separation polish — live progress bar, cancellable download, windowed-sinc resampling, undo on the created tracks |
| **v0.74.0** | Demucs v4 four-stem separation (drums / bass / other / vocals) via ONNX Runtime |
| **v0.73.0** | Windows support for Basic Pitch audio-to-MIDI |
| **v0.72.0** | Polyphonic audio-to-MIDI via Spotify Basic Pitch (ONNX Runtime) |
| **v0.71.0** | Remove dead MIDI menu items; move virtual-keyboard velocity to a transport-bar selector |
| **v0.70.0** | Device preset-name display, scrollable dialogs, marquee over-wide labels; retire `V1MenuBridge` (all menus now native `fw2::MenuEntry`) |
| **v0.69.0** | MIDI LFO named target picker + visual-layer (device / shader) modulation |
| **v0.68.1** | Stop an audio-thread use-after-free when editing / swapping a playing MIDI clip (clone-swap via graveyard) |
| **v0.68.0** | Factory melodic loop library derived from the drum kit |
| **v0.67.1** | Apply the saved BPM on project load |
| **v0.67.0** | MIDI loop browser, save / drag-to-slot, factory drum library |
| **v0.66.1** | Heap-back the live-MIDI buffer to stop an AudioEngine stack overflow |
| **v0.66.0** | Procedural preset generator + scrollable menus |
| **v0.65.0** | Live MIDI record visualization, clip indicators, scene triangles |

For the full per-feature detail, see [docs/features.md](docs/features.md).

## Implementation phases

The early development was organised into numbered phases. Each was
implemented, broken, and fixed in roughly that order.

| Phase | Status | Description |
|---|---|---|
| 1. Project Scaffolding | ✅ Done | CMake build system, SDL3+OpenGL window, directory structure |
| 2. Audio Engine | ✅ Done | PortAudio callback, transport, lock-free ring buffers |
| 3. Clip Playback | ✅ Done | libsndfile loading, quantized clip launching, looping |
| 4. Session View UI | ✅ Done | Clip grid, transport bar, waveform thumbnails, theme |
| 5. Mixer & Routing | ✅ Done | 64-track mixer, 8 send/return buses, master, metering |
| 6. MIDI Engine | ✅ Done | MIDI 2.0-res internals, RtMidi I/O, MPE zones, MIDI clips |
| 7. Metronome | ✅ Done | Synthesized click track, beat-synced, configurable |
| 8. Audio Effects | ✅ Done | Built-in effects + visualizers, effect chains, drag-to-reorder, 3-point insert, sidechain + modulation routing on the `AudioEffect` base |
| 9. Integrated Instruments | ✅ Done | 15 instruments with full UI (SubSynth, FM, Sampler, Karplus-Strong, Wavetable, Granular, Vocoder, String Machine, Drawbar Organ, Electric Piano, Multisampler, InstrumentRack, DrumRack, DrumSlop, DrumSynth) |
| 10. MIDI Effects | ✅ Done | 8 MIDI effects (Arp, Chord, Scale, NoteLength, Velocity, Random, Pitch, LFO) |
| 11. Interactive UI | ✅ Done | Widget system, menu bar, mixer controls, detail panel, virtual keyboard, context menus |
| 12. UI Framework | ✅ Done | Widget tree, FlexBox layout, primitive widgets, dialog system, panel migration |
| 13. Piano Roll | ✅ Done | MIDI note editor with draw/select/erase tools, zoom/scroll, clip integration |
| 14. Composite Widgets | ✅ Done | DeviceWidget, DeviceHeader, FwGrid, VisualizerWidget, SnapScrollContainer, neon knobs |
| 15. Animations & DPI | ✅ Done | Hover animations, panel collapse/expand animations, DPI auto-detection & scaling |
| 16. Arrangement View | ✅ Done | Timeline, clip placement, automation lanes, loop range, waveform display |
| 17. Recording & I/O | ✅ Done | Audio/MIDI recording, MIDI Learn, audio export (WAV/FLAC/OGG), project save/load |
| 18. Session Management | ✅ Done | Scene insert/duplicate/delete, track deletion, follow actions, undo/redo, time stretching |
| 19. VST3 Hosting | ✅ Done | VST3 SDK, plugin scanning, process-isolated editors (Windows HWND + Linux X11 embed with IRunLoop), parameter sync, state persistence |
| 20. Controller Scripting | ✅ Done | Lua 5.4, controller auto-detection, `yawn.*` API, Ableton Push 1 (encoders, display, pads, LEDs) |
| 21. More Controllers | ✅ Done | Ableton Move (32-pad scale grid, ripple LEDs, touch encoders, toast as screen-substitute), Korg nanoKONTROL2 (banked faders/knobs, LED-synced channel buttons, transport row), Yamaha Reface DX (touch-strip → instrument param, CC 7/11 → master/track vol) |
| 22. Visual / VJ Engine | ✅ Done | Per-track GPU layers, Shadertoy-compatible shader hot-reload, video import + live input, glTF 2.0 3D models with skeletal animation, Lua scene scripts, master post-FX chain, A–H knobs + LFOs + automation, arrangement timeline integration |
| 23. Ableton Link | ✅ Done | LAN beat/tempo sync (peers from Live, Logic, Bitwig, iOS apps, etc.) with phase alignment. Local UI tempo edits gated through `localTempoChanged` so the audio thread doesn't clobber typed BPM with the previous-frame's stale session tempo |
| 24. UI Framework Migration | ✅ Done | Three-phase delete-heavy refactor: v1 Widget/FlexBox/EventSystem/UIContext + a 766-line bridge wrapper layer (`PanelWrappers.h`) all retired. Single `fw2::Widget` framework, single `dispatchMouseDown`, single global capture slot. Net ~−2960 lines. Capture-stomp guard added. C4717-and-friends promoted to compile errors |
| 25. Effects Batch II + DrumSynth + Bundled IRs/Models | ✅ Done | Five new effects (Ping-Pong Delay, Spline EQ, Bitcrusher, Noise Gate, Envelope Follower), a Convolution Reverb with full FFT block-convolver + 38 bundled Voxengo IRs, a Neural Amp Modeler integration with 4 bundled `.nam` captures, a fully-synthesised DrumSynth + Piano-Roll DrumRoll mode, and `AudioEffect`-base sidechain + modulation + extra-state plumbing (incl. project-load IR rehydration). RCU-lite atomic engine swap on Conv Reverb + NAM. NAM linked with `$<LINK_LIBRARY:WHOLE_ARCHIVE,nam>` after MSVC silently DCE'd `nam::get_dsp` |
| 26. Period-piece instruments + standalone Phaser/Wah/Rotary | ✅ Done | String Machine (Solina-style ensemble strings), Drawbar Organ (Hammond B-3, auto-pairs with Rotary), and three standalone effects pulled out of inline-synth scope: Phaser, Wah, Rotary |
| 27. Cross-panel drag-drop + audio-clip ops + clip UAF fix | ✅ Done | Drag audio clips from session/arrangement cells onto Sampler / Granular / DrumSlop / Vocoder / DrumRack pads / Multisampler. Crop + Reverse buttons (atomic buffer swap). Project-owned clip graveyard with 5 s TTL covers every slot mutation so the reproducible "delete clip → audio thread reads freed memory" UAF can't recur |
| 28. DrumRack feature parity | ✅ Done | Per-pad audio fx chain (lazy-allocated), per-pad AR envelope, per-pad region trim with reverse playback, choke groups (1–4), kit-preset save/load (one file = one kit) |
| 29. InstrumentRack feature parity | ✅ Done | Default chain on construction, per-chain instrument widget + "Change Instrument →" swap, per-chain effect chain |
| 30. Electric Piano + final instrument round-out | ✅ Done | 2-op FM Electric Piano (Rhodes / Wurli / Suitcase), velocity-driven mod index, per-strike hammer-noise transient, exponential decay |
| 31. Session-record orchestration + stuck-state cure (v0.62) | ✅ Done | Transport Record orchestrates per-track session-record; change-detected clip-state emission cures the "track stays red/green forever" stuck state; `/SUBSYSTEM:WINDOWS` so no stray console window |
| 32. Automation arming + clear scopes + SplineEQ persistence (v0.63) | ✅ Done | Global automation-record arm, clear-automation scopes (all undoable), arrangement loop-marker drag fixes, SplineEQ persistence (per-node-keyed extra-state) |
| 33. Auto-arm UX completion (v0.64) | ✅ Done | Scene-label click runs the per-track record orchestration; per-track MIDI Overdub toggle; per-clip Auto-Rec disable; visual A–H knob touch records breakpoints |

Development past v0.64 is tracked in **Recent releases** above.

## Lessons learned

1. **"It compiles" ≠ "It works"** — But it's a great start when your engineer has no ears
2. **Filter resonance is the QA department** — Crank it up, sweep fast, watch things explode
3. **The AI will always say "Fixed!"** — Statistically, it's right 60% of the time, every time
4. **Lock-free programming is easy** — If you let someone who can't experience race conditions write it
5. **Test counts go both ways** — Because when your codebase is written by autocomplete on steroids, trust but verify. Sometimes you delete tests too. Counting goes up and down
6. **The best bug reports are just vibes** — "After a while the arpeggiator produces notes without me pressing any key" → *chef's kiss*. "When clicking on a second midi track with the synth there is empty space in the detail" → also *chef's kiss*
7. **Track deletion requires stopping the world** — Ableton does it too, so it's a feature not a limitation
8. **MIDI Learn is just "wiggle something, click something"** — The AI understood this perfectly on the 4th attempt
9. **SysEx is where bytes go to hide** — The Push 1 display didn't work for hours because of one missing column offset byte. The PM dug up his own 10-year-old code to prove the AI wrong
10. **Controllers have multiple MIDI ports** — Push 1 sends pads on the "User" port, not the main one. The AI opened the wrong port and wondered why pads were silent
11. **Two UI frameworks side-by-side is one too many** — Two capture slots, two visibility flags, two event types, and a 766-line bridge layer to make them talk. Deleted in three commits. Should have been one. The migration plan went `Phase 1: rename namespace → Phase 2: collapse dispatch → Phase 3: delete the corpse`. We kept Phase 3 a surprise from ourselves
12. **The compiler is trying to tell you something** — `warning C4717: 'fileNameFromPath' is recursive on all control paths, function will cause runtime stack overflow` was in the build output for an unknown number of versions before it actually crashed. It's a compile error now
13. **Member widgets are not children** — `m_scroll` as a value member doesn't get its `invalidate()` to bubble up to the panel. We learned this when a knob's neighbour stopped rendering on the second click. Fix is a one-line `invalidate()` after mutation; the lesson is "lifecycle ownership ≠ tree membership"
14. **Ableton Link's audio-thread API is a foot-gun** — The user types BPM, you set the transport, next audio buffer reads back the OLD session tempo and overwrites your new one. Add a `localTempoChanged` flag. Write the regression test. Walk away
15. **fw2 has a single capture slot** — Two widgets calling `captureMouse()` is one widget overwriting the other's capture. The AI hit this trap so many times that the framework now `assert`s + logs when an ancestor stomps a descendant's capture. The PM appreciates the framework that yells at you when you're about to break it
16. **Audio-thread + UI-thread sharing a `unique_ptr` is a use-after-free waiting for its third opportunity** — Neural Amp's third `.nam` load wrote 0x0 because the audio thread was mid-`process()` on the DSP the UI thread was destroying. Fix: `std::atomic<DSP*>` + retired-list + deferred destruction on the NEXT load (RCU-lite). We then immediately wrote the same trick into Convolution Reverb because the bug was 1 user-event away from happening there too
17. **MSVC's function-level dead-code elimination is a silent linker** — `nam::get_dsp` was in `nam.lib`, the obj file referenced it, the link succeeded, the device showed "idle" with the model path stored. `dumpbin /symbols` on the EXE: not a single `nam::` symbol survived. Fix: `$<LINK_LIBRARY:WHOLE_ARCHIVE,nam>` (CMake 3.24+). Cost is binary size; benefit is the device working
18. **Per-file `CXX_STANDARD 20` collides with a C++17 PCH on a clean build** — and doesn't on incremental ones (the stale `.obj` from before the PCH existed satisfies the link). The first portable `WHOLE_ARCHIVE` attempt passed the AI's local builds and failed CI. PIMPL the C++20 dependency behind a C++17-clean header and the conflict goes away
19. **The 100 MB file is always somewhere** — A git submodule, LFS, a split-in-repo, a release asset — they all move the same ~170 MB Demucs model to a different home with different trade-offs. We bundle it into the release package: the repo stays lean, the download stays self-contained, and the loader prefers the bundled copy over the on-demand cache

*This is what software development looks like in 2026. One human with opinions and one AI with infinite patience. The future is sloppy, it ships, the warnings are errors, the dials turn, and honestly? It kinda slaps.*
